% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_bubble.R
\name{plot_bubble_fc}
\alias{plot_bubble_fc}
\title{Bubble plot of group-vs-control fold-change across FACS populations}
\usage{
plot_bubble_fc(
  data,
  control,
  group_col = "group",
  population_col = "population",
  value_col = "value",
  test = "auto",
  p_adjust_method = "BH",
  summary_fun = mean,
  pseudocount = 0.5
)
}
\arguments{
\item{data}{tibble already filtered to one metric and one tissue; must
contain \code{group_col}, \code{population_col}, \code{value_col}.}

\item{control}{character; value in \code{group_col} treated as the
baseline. Every other distinct value is compared against it.}

\item{group_col}{character; column in \code{data} holding group labels.
Default \code{"group"}.}

\item{population_col}{character; column in \code{data} holding
population names. Default \code{"population"}.}

\item{value_col}{character; column in \code{data} holding the numeric
measurement. Default \code{"value"}.}

\item{test}{character; one of \code{"auto"} (default), \code{"wilcox"},
\code{"t.test"}, \code{"kruskal"}. \code{"auto"} uses pairwise
\code{stats::wilcox.test()} when there are 2 groups (incl. control) and
\code{stats::kruskal.test()} + \code{rstatix::dunn_test()} post-hoc when there
are more than 2.}

\item{p_adjust_method}{character; passed to \code{stats::p.adjust()}
(wilcox/t.test) or \code{rstatix::dunn_test()}'s own
\code{p.adjust.method} (kruskal). Default \code{"BH"}.}

\item{summary_fun}{function; aggregator applied to each group's values
when computing fold-change. Default \code{mean}.}

\item{pseudocount}{numeric; added to both summary values before
dividing, avoiding \code{log2(0)}/\code{Inf}. Default \code{0.5}.}
}
\value{
A \code{ggplot} object.
}
\description{
Compares one or more groups against an explicitly named control group,
one column per group-vs-control comparison and one row per population.
Each point is colored and sized by the log2 fold-change of the group's
mean value versus the control's, and annotated with a significance-star
summary from \code{test}. \code{data} must already be filtered to a single
\code{metric} and a single \code{tissue} (if those columns are present).
}
\examples{
\dontrun{
  dat <- facs_read_wsp("experiment.wsp", keywords = c("group"))$data |>
    dplyr::filter(metric == "FractionOfParent", tissue == "Spleen")
  plot_bubble_fc(dat, control = "control")
}
}
